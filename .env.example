# Supabase - Add these to enable backend (comments, real data)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# XUMM: API key from https://apps.xumm.dev (for Connect Wallet + payment flow)
# Add allowed origins in Xumm Console: https://your-app.vercel.app, http://localhost:5173
# VITE_XUMM_API_KEY=your_xumm_app_api_key

# Treasury: XRPL address for 1 XRP submission fee (optional)
# VITE_TREASURY_WALLET=rYourTreasuryAddress...

# XRPL network: "testnet" for test XRP, omit for mainnet
# VITE_XRPL_NETWORK=testnet

# Admin: Password for /admin moderation queue (add to Vercel env too)
# ADMIN_PASSWORD=your_secret_password

# Vercel: Token from vercel.com/account/tokens (for scripts/add-vercel-env.js)
# VERCEL_TOKEN=your_vercel_token
# VERCEL_PROJECT=BOP

# Optional: For running schema via npm run db:schema
# Get from: Supabase Dashboard > Project Settings > Database
# SUPABASE_DB_PASSWORD=your_database_password
